/**
 * module is the functions or variables that can be stored in the file 
 * 
 * there are two types of export 
 * 1) named export 
 * 2) default export 
 * 
 * named export are constructed by curly braces.
 * defaut export are constructed normal
 */